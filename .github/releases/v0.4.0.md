# 瞬间 Lumix v0.4.0

本版本将 Android applicationId 和 Java 包名迁移到 `cn.moment.lumix`，RN 原生模块名统一为 `MomentLumix`。

这是全新包名，旧版 `cn.moment.s5` 不会原地升级；安装前后两个包可以并存，按需手动卸载旧版。

当前相机驱动仍面向 Panasonic S5，S5M2 及其他机型适配沿用新的包名和桥接入口继续开发。
