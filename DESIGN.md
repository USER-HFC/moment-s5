# 产品与交互

名字：瞬间 S5。主操作：一根线连接、按一次实况快门、在片刻中长按回放。

## 视觉

使用原生 Android View，无网页容器。暗色背景 `#101210`、表面 `#1B201B`、正文 `#EDF0E7`、辅助文字 `#B0B9AB`、强调 `#D6F58B`。正文与表面均以高对比阅读为优先；数字使用系统等宽字体。拍攝按钮圆形、其他操作为有间距的圆角按钮。无网络字体、装饰动效或广告。

UI/UX Pro Max 的两次设计检索偏向网站，不适合作为相机布局模板。只采用其通用原生界面准则：Android 至少 48dp 点击区、原生语义控件、可滚动内容、系统安全区、大字体与横屏可达性。布局依据实际拍摄流程设计。

## 页面

- 拍摄：取景 → 曝光信息 → 预缓存进度 → 近/远/自动对焦 → 实况快门。未连接时按钮不可拍摄，演示必须明确进入。
- 片刻：按时间倒序的本地作品；未完成的合成标出错误，原片仍可分享。
- 连接：三步连接说明、USB 授权、参数刷新、收音开关和可分享的诊断。
- 详情：原片 → 长按动态 → 独立播放按钮 → 帧间隔与声音状态 → 相册导出/分享。

## 状态与真实性

未连接 → 授权中 → 会话建立 → 预缓存 → 可拍 → 快门/后缓存 → 原片接收 → 合成 → 保存。

每个媒体任务串行执行。普通 USB 响应错误会提示；传输失败会废弃当前会话，必须重连，不自动重发快门。不会用旧照片替代未收到的新照片。拍照事件监听在发出快门前建立，事件失败时只允许使用与已知基线不同的照片句柄。

演示记录必须携带 `demo:true`，UI 显示“演示”，图片本身含 SYNTHETIC DEMO 标记。真机拍照从不退化成模拟照片。

## 核心验收

已连接的受支持机身：点一次 App 快门 → 得到原尺寸 JPEG + 约三秒动态 → App 长按可播放 → 可导出 Motion Photo。

能够独立完成的验证包括构建、解析器、媒体编解码、存储和界面；真实 USB 行为、ColorOS 原生识别、镜头对焦精度及音画同步必须依靠用户的实体设备。
