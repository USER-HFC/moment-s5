# 产品与交互

名字：瞬间 Lumix。首页四入口：监看、定时遥控、动态照片、相册。使用 sensorLandscape，手机两个横向均可用；品牌不绑定单一机型。

## 视觉

使用原生 Android View，无网页容器。暗色背景 `#101210`、表面 `#1B201B`、正文 `#EDF0E7`、辅助文字 `#B0B9AB`、强调 `#D6F58B`。保留高对比和系统字体，使用原生圆角按钮及按压波纹，不添加装饰动画或运行时依赖。

UI/UX Pro Max 的两次设计检索偏向网站，不适合作为相机布局模板。只采用其通用原生界面准则：Android 至少 48dp 点击区、原生语义控件、可滚动内容、系统安全区、大字体与横屏可达性。布局依据实际拍摄流程设计。

## 页面

- 首页：2 × 2 功能菜单，顶部连接状态与设置入口。功能页保留首页返回和当前页名称，不占用横屏高度放底部导航。
- 监看：左侧自适应实时画面，右侧网格开关、曝光信息、对焦和连接操作。
- 定时遥控：左侧取景，右侧 2 / 5 / 10 / 30 秒延时选择与开始/取消；单张照片仅由机身保存。未连接或忙碌时不可开始。
- 动态照片：左侧取景，右侧前 3 秒缓存进度、动态快门和对焦；保留旧版媒体流程。
- 相册：按时间倒序的本地作品；未完成的合成标出错误，原片仍可分享。不是机身存储卡浏览器。
- 连接：三步连接说明、USB 授权、参数刷新、收音开关和可分享的诊断。
- 详情：左侧原片/动态，右侧独立播放按钮、帧间隔与声音状态、导出/分享。操作面板可滚动，四边系统安全区保留。

## 状态与真实性

未连接 → 授权中 → 会话建立 → 快门前 3 秒预缓存 → 可拍 → 索引读取/必要时补帧 → 固定前缓存并触发快门 → 原片接收 → 合成 → 保存。

每个媒体任务串行执行。普通 USB 响应错误会提示；传输失败会废弃当前会话，必须重连，不自动重发快门。不会用旧照片替代未收到的新照片。拍照事件监听在发出快门前建立，事件失败时只允许使用与已知基线不同的照片句柄。

倒计时使用单调时钟，仅前台当前页有效；切页、返回、退出前台、拔线均取消未到点任务。到点只派发一次遥控指令，不自动重试，不代表已确认机身成功存盘。横向翻转只重排布局，不额外触发快门。未实现间隔连拍、后台任务、机身相册下载等扩展。

演示记录必须携带 `demo:true`，UI 显示“演示”，图片本身含 SYNTHETIC DEMO 标记。真机拍照从不退化成模拟照片。

## 核心验收

已连接的受支持机身：点一次 App 快门 → 得到原尺寸 JPEG + 约三秒动态 → App 长按可播放 → 可导出 Motion Photo。

能够独立完成的验证包括构建、解析器、媒体编解码、存储和界面；真实 USB 行为、ColorOS 原生识别、镜头对焦精度及音画同步必须依靠用户的实体设备。
